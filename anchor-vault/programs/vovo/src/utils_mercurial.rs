use solana_program::{
    program::{ invoke_signed},
    entrypoint::ProgramResult,
};

pub fn mercurial_add_liquidity()->ProgramResult{
    Ok(())
}
pub fn mercurial_remove_liquidity()->ProgramResult{
    Ok(())
}
pub fn mercurial_remove_liquidity_one_token()->ProgramResult{
    Ok(())
}
pub fn mercurial_exchange()->ProgramResult{
    Ok(())
}